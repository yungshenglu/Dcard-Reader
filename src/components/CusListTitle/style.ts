import styled from 'styled-components';

export const StyledCusListTitle = styled.div`
  height: 30px;
  width: 100%;
  background: #006AA6;
  position: fixed;
  z-index: 100;
`;

export const StyledCusListTitleText = styled.h1`
  color: #fff;
  font-size: 0.8rem;
  font-weight: normal;
  padding: 8px 40px;
  text-align: center;
  -webkit-box-shadow: 0 3px 5px 0 rgba(36,87,117,0.5);
	-moz-box-shadow: 0 3px 5px 0 rgba(36,87,117,0.5);
	box-shadow: 0 3px 5px 0 rgba(36,87,117,0.5);
`;
